<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moviedb">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieDB">
        <activity
            android:name=".view.MainMenuActivity"
            android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
        </activity>

        <activity
            android:name=".adapter.MovieByGenreAdapter"
            android:exported="true" />
        <activity
            android:name=".view.activites.MovieByGenreActivity"
            android:exported="true" />
        <activity
            android:name=".view.activites.MovieDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".view.activites.NowPlayingActivity"
            android:exported="true"/>
        <activity
            android:name=".view.activites.MainActivity"
            android:exported="true" />
    </application>

</manifest>